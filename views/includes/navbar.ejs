<nav
  class="sticky top-0 z-60 bg-white/80 backdrop-blur-md border-b border-gray-200"
>
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-20 items-center justify-between">
      <!-- Left: Logo -->
      <div class="flex items-center gap-2">
        <svg
          class="h-8 w-8 text-rose-500"
          fill="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            d="M12 2C8 2 5 5 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-4-3-7-7-7zm0 9.5a2.5 2.5 0 1 1 0-5 2.5 2.5 0 0 1 0 5z"
          />
        </svg>
          <span class="text-2xl font-semibold text-gray-900 tracking-tight">
          Wander<span class="text-rose-500">Lust</span>
        </span>
      </div>

      <!-- Desktop Links -->
      <div class="hidden md:flex items-center gap-8">
        <a
          href="/listings"
          class="text-xl font-medium text-gray-700 hover:text-gray-900"
          >Explore</a
        >
        <a
          href="/listings"
          class="text-xl font-medium text-gray-700 hover:text-gray-900"
          >Listings</a
        >
        <a
          href="/listings/new"
          class="text-xl font-medium text-gray-700 hover:text-gray-900"
          >Become a Host</a
        >
      </div>

      <!-- Right -->
      <div class="flex items-center gap-4">
        <button class="hidden sm:inline-flex text-xl font-medium text-gray-700">
          Login
        </button>

        <a
          href="/signup"
          class="hidden sm:inline-flex rounded-full bg-rose-500 px-4 py-2 text-xl text-white"
        >
          Sign up
        </a>

        <!-- Mobile Menu Button -->
        <button onclick="toggleMenu()" class="md:hidden">
          <svg
            class="h-7 w-7 text-gray-800"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-width="2"
              stroke-linecap="round"
              d="M4 6h16M4 12h16M4 18h16"
            />
          </svg>
        </button>
      </div>
    </div>
  </div>
</nav>

<!-- Mobile Sidebar -->
<div id="mobileMenu" class="fixed inset-0 z-50 hidden">
  <!-- Overlay -->
  <div onclick="toggleMenu()" class="absolute inset-0 bg-black/40"></div>

  <!-- Sidebar -->
  <div class="absolute right-0 top-0 h-full w-64 bg-white p-6 space-y-6">
    <button onclick="toggleMenu()" class="mb-4 text-gray-500">✕ Close</button>

    <a href="/" class="block text-lg font-medium text-gray-800">Explore</a>
    <a href="/listings" class="block text-lg font-medium text-gray-800"
      >Listings</a
    >
    <a href="/listings/new" class="block text-lg font-medium text-gray-800"
      >Become a Host</a
    >

    <hr />

    <a href="/login" class="block text-gray-700">Login</a>
    <a href="/signup" class="block text-rose-500 font-semibold">Sign up</a>
  </div>
</div>

<!-- JS -->
<script>
  function toggleMenu() {
    document.getElementById("mobileMenu").classList.toggle("hidden");
  }
</script>
