<% layout('/layouts/boilerplate.ejs') %>
<main class="min-h-[90vh] bg-gray-100">
  <!-- Listing Card -->
  <div class="flex justify-center px-4 sm:px-6">
    <div class="w-full px-4 sm:px-5 sm:w-3/4 md:w-2/3 lg:w-1/2 py-6">
      
      <!-- Heading -->
      <h3 class="text-xl sm:text-2xl md:text-3xl font-bold text-gray-800 mb-4">
        <%= listing.title %>
      </h3>

      <!-- Image -->
      <div class="w-full">
        <img
          src="<%= listing.image %>"
          alt="image"
          class="w-full h-52 sm:h-56 md:h-64 rounded-2xl object-cover"
        />
      </div>

      <!-- Details -->
      <div class="py-4 space-y-2">
        <p class="text-gray-700 text-sm sm:text-base line-clamp-2">
          <%= listing.description %>
        </p>

        <p class="text-gray-900 font-semibold text-base sm:text-lg">
          ₹<%= listing.price.toLocaleString('en-IN') %>/Night
        </p>

        <p class="text-gray-700 text-sm sm:text-base">
          <%= listing.location %>, <%= listing.country %>
        </p>
      </div>

      <!-- Buttons -->
      <div class="flex flex-col sm:flex-row gap-3">
        <a
          href="/listings/<%= listing._id %>/edit"
          class="w-full  text-center bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg font-medium transition"
        >
          Edit
        </a>

        <a
          href="/listings"
          class="w-full text-center bg-gray-600 hover:bg-gray-700 text-white px-6 py-2 rounded-lg font-medium transition"
        >
          Back
        </a>

        <form
          action="/listings/<%= listing._id %>?_method=DELETE"
          method="POST"
          class="w-full "
        >
          <button
            class="w-full bg-red-600 hover:bg-red-700 text-white px-6 py-2 rounded-lg font-medium transition"
          >
            Delete
          </button>
        </form>
      </div>
    </div>
  </div>

  <!-- Review Form -->
  <div class="flex justify-center px-4 sm:px-6">
    <div class="w-full px-4 sm:px-5 sm:w-3/4 md:w-2/3 lg:w-1/2 py-6">
      <form
        action="/listings/<%= listing._id %>/review"
        method="post"
        class="w-full space-y-4"
      >
        <!-- Title -->
        <h2 class="text-lg sm:text-xl font-semibold text-gray-800">
          Add Your Review
        </h2>

        <!-- Range -->
        <div>
          <label class="block text-sm text-gray-700">
            Rating
          </label>
          <input
            type="range"
            min="1"
            max="5"
            id="rating"
            name="review[rating]"
            class="w-full"
          />
        </div>

        <!-- Textarea -->
        <div>
          <label class="block text-sm text-gray-700 mb-1">
            Comment
          </label>
          <textarea
            rows="4"
            placeholder="Write your review here..."
            name="review[comment]"
            id="comment"
            class="w-full bg-gray-100 border rounded border-gray-300 px-3 py-2 text-sm sm:text-base focus:outline-none focus:border-gray-400 resize-none"
          ></textarea>
        </div>

        <!-- Button -->
        <button
          type="submit"
          class=" border border-gray-800 rounded px-4 py-2 text-sm  hover:bg-gray-800 hover:text-white transition"
        >
          Submit
        </button>
      </form>
    </div>
  </div>
</main>
